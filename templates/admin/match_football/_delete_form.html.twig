<form method="post" action="{{ path('app_match_football_delete', {'id': match_football.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match_football.id) }}">
    <button class="btn">Delete</button>
</form>
